// Module 2 — Office Tools (Word, Excel, PowerPoint)
// Bloom's-aligned MCQs. Each topic follows the progression:
// Q1 Remember · Q2 Understand · Q3 Apply · Q4 Apply · Q5 Analyze · Q6 Evaluate · Q7 Create
// All distractors are Grade A: every option is plausible.

import type { Question } from "./module1-mcq";

export const mcqData: Record<number, Question[]> = {
  // ─── Topic 1: Editing vs Word Processing ───
  1: [
    { q: "Which statement most precisely distinguishes a text editor from a word processor?", opts: ["They are synonyms for the same software category", "A text editor manipulates plain characters only; a word processor adds formatting, layout, images, tables, and styling on top of editing", "A word processor saves smaller files than a text editor", "Only text editors can open .docx files"], ans: 1, why: "Editing is the common ground (type, delete, copy). Word processing is editing PLUS everything else. The relationship is a superset, not two separate tools.", bloom: "remember" },
    { q: "Why do programmers often prefer a plain text editor like Notepad or VS Code over MS Word for writing code?", opts: ["Word is too slow", "Code must be exact plain text — invisible formatting characters (smart quotes, auto-capitalised letters, embedded styles) that word processors insert can silently break code", "Programmers don't like icons", "Code files are too big for Word"], ans: 1, why: "Understanding the tool's fit means seeing that Word's \"features\" (auto-correct, smart quotes) are bugs for code. A plain text editor guarantees byte-for-byte what you typed — exactly what compilers need.", bloom: "understand" },
    { q: "Your professor accepts only `.docx` submissions with a header, page numbers, and a table of contents. Applying what you know, which tool fits?", opts: ["Notepad — it's the simplest option", "A word processor (MS Word, Google Docs, or LibreOffice Writer) — headers, pagination, and TOC are word-processing features that plain editors don't support", "Either — they produce the same file", "Any, but save as .txt"], ans: 1, why: "The required features (headers, pagination, TOC) only exist in word processors. Applying what you know means matching submission requirements to tool capability.", bloom: "apply" },
    { q: "You receive a 2000-word document as a `.txt` file. The content is fine, but it looks \"flat.\" What's happening?", opts: ["The file is corrupted", "`.txt` stores plain characters only — any bold/italic/colour/heading applied by the original author is stripped on save because the format has no way to represent formatting", "Your computer is slow", "Only .docx files can hold text"], ans: 1, why: "Applying format knowledge: `.txt` = characters only, no styling metadata. Anything visual the author did is gone the moment they saved as .txt. The content survives; the presentation doesn't.", bloom: "apply" },
    { q: "A student submits a 10-page essay written in Notepad and saved as .txt. Their professor says the formatting is \"unreadable.\" Analyse what the student likely failed to understand about the tool.", opts: ["Notepad has a secret formatting mode", "The student didn't distinguish editing (entering text) from word processing (presenting text). Notepad can hold the words but can't produce headings, consistent fonts, spacing, or pagination the professor expected", "Professors don't like .txt for political reasons", "Notepad can't save 10 pages"], ans: 1, why: "The failure mode is conceptual, not technical. Analysis means naming the gap: the student used a tool sized for characters when the task required presentation. That's the editing vs word-processing distinction in practice.", bloom: "analyze" },
    { q: "A company standardises on Google Docs for everything and bans plain text editors entirely. Evaluate this policy.", opts: ["Great — Docs does everything", "Flawed for roles that handle code, config files, CSVs, or log files — all of which Docs can corrupt by auto-formatting. The policy serves writers but creates friction for engineers, finance (raw CSV), and ops", "Great — consistency is all that matters", "Flawed only because Docs needs internet"], ans: 1, why: "Evaluating a policy means testing it against actual roles. A word processor is wrong for plain-text roles. A blanket ban ignores that some jobs need exactness, not presentation.", bloom: "evaluate" },
    { q: "Design a file-format policy for a university department that handles: research papers, course notes, code samples, and meeting minutes. Which format(s) would you require?", opts: ["Everything as .docx", "Papers: .docx/.pdf (rich formatting). Course notes: .docx or Markdown. Code: .txt or language-specific extensions, never .docx. Minutes: .docx or Markdown. Each format chosen for fidelity to its content", "Everything as .txt", "Everything as PDF only"], ans: 1, why: "Designing a policy means mapping content type to format capability. Code in .docx corrupts; papers in .txt lose structure. A one-size answer fails; a per-content answer shows you understand the editing vs word-processing distinction in practice.", bloom: "create" },
  ],

  // ─── Topic 2: Word Formatting (Ribbon, shortcuts, alignment) ───
  2: [
    { q: "What is the Ribbon in Microsoft Word?", opts: ["The document outline at the left", "The tabbed strip at the top that groups related tools (Home, Insert, Layout, etc.)", "The bottom status bar showing word count", "The scroll bar along the right edge"], ans: 1, why: "The Ribbon is Word's primary tool container — tabs across the top, each with groups of related buttons. The other answers describe different UI elements.", bloom: "remember" },
    { q: "Why does Word (and most apps) require you to SELECT text before applying bold, italic, or a different font?", opts: ["Word is old and requires extra steps", "Commands act on a target. Without a selection, Word has no target; with a selection, the command knows exactly what range to modify. It's an unambiguous contract between user and program", "Selection is optional — formatting works without it", "Selection makes Word faster"], ans: 1, why: "Understanding UI means seeing that \"select → command\" is a general pattern, not a Word quirk. Any command that changes content has to know WHAT content it's changing.", bloom: "understand" },
    { q: "A heading should stretch evenly across the full line width, with both left and right edges perfectly aligned. Which alignment and shortcut?", opts: ["Centre (Ctrl+E)", "Justify (Ctrl+J)", "Right (Ctrl+R)", "Left (Ctrl+L)"], ans: 1, why: "Applying formatting knowledge: Justify stretches word spacing so both edges align. Centre keeps the line centred but doesn't stretch. Left/right align only one edge.", bloom: "apply" },
    { q: "You've formatted a paragraph exactly how you want it (font, colour, spacing, size) and need to apply the same styling to six other paragraphs fast. What's the most efficient approach?", opts: ["Retype every paragraph with the same settings", "Use Format Painter: click on the formatted paragraph, then click/drag across the target paragraphs — it copies formatting without changing content", "Copy and paste each paragraph then edit content back", "Change each paragraph manually one at a time"], ans: 1, why: "Applying knowledge of Word's tools means picking the one built for the job. Format Painter is literally designed to duplicate formatting across selections — faster and more consistent than any manual approach.", bloom: "apply" },
    { q: "A document uses manual bold+size+font combinations on every heading instead of the built-in Heading 1/Heading 2 styles. Analyse the downstream consequences.", opts: ["No consequences — it looks the same", "Problems: inconsistent styling across headings, can't auto-generate a Table of Contents, no navigation pane, and restyling the whole document means editing every heading individually. Styles solve all four", "The document will get corrupted", "Word will refuse to save the file"], ans: 1, why: "Analysing means tracing the ripple effects. Styles aren't just formatting — they're metadata. Abandoning them forfeits TOC generation, navigation, global restyling, and outline view.", bloom: "analyze" },
    { q: "A team debates whether to ban inline formatting in favour of Styles-only. Evaluate.", opts: ["Inline formatting is always fine", "Styles-only is better for long/structured documents (reports, dissertations): enables TOC, outline navigation, and global restyle. Inline is acceptable for short documents. A total ban may be too strict for casual notes, but recommending Styles for structured work is correct", "Ban styles — inline is simpler", "Both are identical in practice"], ans: 1, why: "Evaluating means acknowledging the trade-off. Styles pay off in proportion to document length and structure. A one-page note doesn't need them; a 50-page report does. A nuanced policy beats a blanket rule.", bloom: "evaluate" },
    { q: "Design a style system for a 100-page university thesis. Name the styles you would define and explain why each exists.", opts: ["Only one style for everything", "Title, Heading 1 (chapters), Heading 2 (sections), Heading 3 (subsections), Body, Caption (for figures/tables), Block Quote, Code. Each style encodes a rule so the document is globally restyleable and the TOC auto-generates. Naming matches Word's built-in hierarchy so it \"just works\"", "One style per page", "Bold and italic, nothing else"], ans: 1, why: "Designing a style system means enumerating the structural roles a thesis uses and giving each a named style. A flat \"one style\" approach loses all the structural benefits; this list captures the actual document anatomy.", bloom: "create" },
  ],

  // ─── Topic 3: Images and Tables in Word ───
  3: [
    { q: "Which Ribbon tab hosts the tools for inserting images, tables, shapes, and charts?", opts: ["Home", "Insert", "Layout", "View"], ans: 1, why: "The Insert tab is the designated home for adding anything that isn't plain typed text. Home is formatting; Layout is page setup; View is display preferences.", bloom: "remember" },
    { q: "Why should you drag a CORNER handle (not a side handle) when resizing an image?", opts: ["Corner handles are more reliable", "Corner handles preserve the aspect ratio (width-to-height relationship); side handles stretch only one dimension, distorting the image", "Side handles don't work in modern Word", "There is no difference"], ans: 1, why: "Understanding image controls: corner = proportional resize, side = stretch. The visible difference shows up immediately on photos — stretched people get visibly squashed.", bloom: "understand" },
    { q: "You insert a company logo at the top of a page and want the paragraph text to flow around it on the right. Which text wrapping setting?", opts: ["In Line with Text", "Square (text wraps around the image's bounding box)", "Behind Text", "In Front of Text"], ans: 1, why: "Applying wrap options: \"In Line\" treats the image as a big character (breaks flow). \"Square\" lets text wrap neatly. \"Behind/In Front\" overlaps, which is usually wrong for a logo + text layout.", bloom: "apply" },
    { q: "You need a schedule table: 7 columns (days) and 10 rows (hour slots), with header highlighting. What's the most reliable way to build it?", opts: ["Draw each line with the pen tool", "Insert → Table → Insert Table (specify 7×10); then select the header row and apply shading from the Table Design tab", "Use spaces and the Enter key to align columns", "Export from Excel only"], ans: 1, why: "Applying Word knowledge: the Insert Table dialog lets you specify exact dimensions (grid mode caps at 10×8). Styling the header via Table Design is the standard path.", bloom: "apply" },
    { q: "A student uses a table with invisible borders to lay out their whole page (navigation row, content row, footer row). Analyse why this is considered bad practice today.", opts: ["It isn't — it works fine", "Tables are semantically for tabular data. Using them for layout confuses screen readers (\"row 1 contains 3 cells of unrelated content\"), breaks copy-paste ordering, and fights modern responsive design. The visual looks fine; the semantics are wrong", "Tables only work in HTML, not Word", "It will crash Word"], ans: 1, why: "Analysing means distinguishing visual output from document semantics. Tables-for-layout is a historical web hack that also hurts Word document accessibility. The file opens, but assistive tech misreads it.", bloom: "analyze" },
    { q: "A designer argues that every image in a report should use \"In Line with Text\" wrapping because it's \"simplest and least likely to break.\" Evaluate.", opts: ["Always correct — In Line is safest", "Partly correct. In Line IS the most predictable because the image behaves like a character — safe for small inline icons. For large figures you want text to flow around, Square/Tight is better. Rule of thumb, not absolute", "Always wrong — never use In Line", "Doesn't matter which you pick"], ans: 1, why: "Evaluating requires accepting the partial truth. \"Simplest\" is often right, but \"always\" is too strong. The correct answer acknowledges In Line's safety while naming when to deviate.", bloom: "evaluate" },
    { q: "Design the table structure for a 3-page course syllabus that lists week number, topic, readings, and assignments. How do you structure the table, and how do you ensure it prints well across pages?", opts: ["One giant cell with all the data", "A 4-column table (Week, Topic, Readings, Assignments) with a styled header row; enable \"Repeat Header Rows\" in Table Properties so the header re-prints on each page; use consistent cell padding", "A table per row", "Paragraphs with spaces"], ans: 1, why: "Designing a data table means naming the columns, styling the header, and solving the print-flow problem (headers repeating). A simple-looking answer that ignores the page-break problem isn't a complete design.", bloom: "create" },
  ],

  // ─── Topic 4: Excel — Cells, Rows, Columns, Sheets ───
  4: [
    { q: "What does \"cell C5\" refer to in Excel?", opts: ["The 5th cell of column C", "The cell at the intersection of column C and row 5", "Row C, column 5", "The 5th sheet named C"], ans: 1, why: "Excel addresses are [column letter][row number]. C = third column. 5 = fifth row. Always column first, then row.", bloom: "remember" },
    { q: "Why does Excel organise data into separate Worksheets within a single Workbook, instead of just one giant sheet?", opts: ["It's a limitation of older Excel versions", "Separation: different topics (sales, inventory, forecasts) get their own sheet with their own structure, while sharing one file. Linking formulas can pull data across sheets when needed", "Worksheets can't actually hold much data", "Workbooks are a legacy feature"], ans: 1, why: "Understanding workbook vs worksheet means seeing it as a file-and-tabs pattern: one file for organisation, many sheets for logical separation. Formulas bridge sheets when you need cross-sheet data.", bloom: "understand" },
    { q: "You need to sum cells from B2 through B30. Which range notation is correct?", opts: ["=SUM(B2, B30)", "=SUM(B2:B30)", "=SUM(B2-B30)", "=SUM(B2+B30)"], ans: 1, why: "Applying range syntax: the colon creates a range (all cells from B2 to B30 inclusive). Comma = list of individual cells. Dash/plus = arithmetic, not range.", bloom: "apply" },
    { q: "You entered \"45\" into A1 but it left-aligns and won't sum. What's happening and how do you fix it?", opts: ["Excel is broken", "The cell is formatted as Text, so \"45\" is treated as characters, not a number. Fix: select the cell → Format Cells → Number (or retype the number in a fresh General-formatted cell)", "45 is too small a number", "The keyboard is in the wrong language"], ans: 1, why: "Applying formatting knowledge: left-alignment + refusing to sum = classic \"looks like a number but is text.\" Re-formatting to Number (or using =VALUE()) fixes it.", bloom: "apply" },
    { q: "A workbook has three sheets: Sales, Expenses, Summary. The Summary sheet pulls totals from the other two using `=Sales!B10` and `=Expenses!B10`. Someone renames \"Sales\" to \"Q1 Sales.\" Analyse what happens.", opts: ["Nothing — Excel figures it out", "Excel automatically updates references within the file IF the rename is done via the tab itself. If the sheet was renamed externally or the file is opened in an older app, the references break and show `#REF!`. Spaces in names also require single quotes: `='Q1 Sales'!B10`", "All formulas break forever", "The file gets corrupted"], ans: 1, why: "Analysing cross-sheet references means tracing how they survive edits. Tab-rename within Excel updates references; external renames or bad syntax (spaces without quotes) break them. Real knowledge of the mechanism, not just syntax.", bloom: "analyze" },
    { q: "A teacher puts 300 students in a single worksheet with 50 columns of data per student. Evaluate whether this is a good structure.", opts: ["Perfect — all data in one place", "Reasonable for 300 rows (Excel handles millions), but 50 columns is the real concern: if columns represent different categories (grades, attendance, demographics), splitting into multiple sheets or using a proper database is easier to maintain. Evaluate per-column purpose, not just row count", "Terrible — Excel can't hold 300 rows", "Should be on 300 separate sheets"], ans: 1, why: "Evaluating structure requires naming the real constraint. Row count isn't the issue; column sprawl and mixed purposes are. A thoughtful answer addresses the actual pain point.", bloom: "evaluate" },
    { q: "Design the workbook structure for a small business tracking orders, customers, and products. How many sheets, and how do they relate?", opts: ["Everything on one sheet", "Three sheets: Customers (ID, name, email), Products (ID, name, price), Orders (order ID, customer ID, product ID, qty, date). Orders references the other two by ID. This mirrors a relational database and keeps each sheet focused", "One sheet per order", "No structure — random"], ans: 1, why: "Designing means normalising data: each entity gets its own sheet, with references by ID. A single-sheet blob duplicates customer/product data on every order row; a per-order sheet is unusable. The three-sheet structure is the industry pattern.", bloom: "create" },
  ],

  // ─── Topic 5: Excel Formulas (SUM, AVERAGE, IF, MIN, MAX, COUNT) ───
  5: [
    { q: "Every Excel formula must start with which character, and why?", opts: ["A number, so Excel treats it as data", "An equals sign (=), which tells Excel to evaluate what follows instead of storing it as text", "A quotation mark, to group it", "A hashtag, to categorise it"], ans: 1, why: "= is Excel's signal that \"what follows is a formula, not a label.\" Without it, `A1+B1` displays as literal text.", bloom: "remember" },
    { q: "Why does `=AVERAGE(A1:A10)` give a different result from `=SUM(A1:A10)/10` if some cells in A1:A10 are empty?", opts: ["They are always identical", "AVERAGE divides by the count of NUMERIC cells; /10 divides by 10 regardless. If 3 of the 10 cells are empty, AVERAGE divides by 7 and /10 divides by 10 — two different answers", "SUM ignores empty cells; AVERAGE doesn't", "Only AVERAGE works with ranges"], ans: 1, why: "Understanding the function means knowing what it counts. AVERAGE = sum / count-of-numbers. The /10 hardcodes a denominator that may not match reality. For data with blanks, AVERAGE is correct; /10 is a bug.", bloom: "understand" },
    { q: "A teacher has scores in B2:B31 and needs to count how many are 40 or above (pass). Which formula is correct?", opts: ["=COUNT(B2:B31)", "=COUNTIF(B2:B31, \">=40\")", "=SUMIF(B2:B31, \">=40\")", "=IF(B2:B31>=40, 1, 0)"], ans: 1, why: "Applying the right function: COUNT counts all numeric cells. SUMIF adds values meeting a condition (gives sum, not count). COUNTIF counts cells meeting a condition — exactly what's asked.", bloom: "apply" },
    { q: "Cell D1 = `=IF(A1>100, \"Gold\", IF(A1>50, \"Silver\", \"Bronze\"))`. A1 contains 75. What does D1 show, and why?", opts: ["Gold, because 75 is a large number", "Silver. The first IF checks 75>100 (false), falls to inner IF which checks 75>50 (true), returns \"Silver\"", "Bronze — nothing matched", "#ERROR — nested IFs aren't allowed"], ans: 1, why: "Applying nested IF: conditions are checked in order, and the first true one wins. Real knowledge means tracing the logic step by step, not eyeballing.", bloom: "apply" },
    { q: "A payroll sheet uses `=B2*C2` for every row, copied down 500 rows. Someone adds a new \"Bonus\" column between B and C, shifting C to D. Analyse what happens to the formulas.", opts: ["Nothing — formulas break", "Excel automatically updates the references: the formulas become `=B2*D2` because Excel tracks cell identity through column insertions. But any NEW C column content is ignored — the formulas still multiply original columns, not the new structure. This is correct behaviour, but easy to misread as a bug", "All formulas point to the same cell", "Excel refuses to insert the column"], ans: 1, why: "Analysing formula behaviour under structural changes is core spreadsheet literacy. Excel preserves reference semantics (not positions), which is usually right but can mislead people who think of formulas as position-based.", bloom: "analyze" },
    { q: "A team uses `=IF(score>=60, \"Pass\", \"Fail\")` everywhere. A manager proposes switching to `=IFS(score>=85, \"A\", score>=70, \"B\", score>=60, \"C\", TRUE, \"F\")`. Evaluate.", opts: ["The simple IF is always better", "IFS is better for multi-branch logic: cleaner than deeply nested IFs, easier to read, and scales to 5+ conditions. For just two outcomes (Pass/Fail), IF is fine — it depends on the decision shape", "Always use the longest formula", "They're identical — pick any"], ans: 1, why: "Evaluating formula choice means matching structure to decision shape. Binary → IF. Multi-branch → IFS (or nested IF, if forced). The right answer names the criterion, not a default.", bloom: "evaluate" },
    { q: "Design a set of formulas for a grade sheet with 30 students' scores in B2:B31. You need: average, highest, lowest, number of passes (≥60), percentage passing, and a letter-grade column.", opts: ["Just use SUM for everything", "Average: =AVERAGE(B2:B31). Highest: =MAX(B2:B31). Lowest: =MIN(B2:B31). Passes: =COUNTIF(B2:B31,\">=60\"). % passing: =COUNTIF(B2:B31,\">=60\")/COUNT(B2:B31). Letter grade (in C2, dragged): =IFS(B2>=85,\"A\",B2>=70,\"B\",B2>=60,\"C\",TRUE,\"F\")", "Write the answers by hand", "Use =TOTAL for everything"], ans: 1, why: "Designing a formula suite means choosing the right function per question. The full design uses 5 distinct functions (AVERAGE, MAX, MIN, COUNTIF, IFS) each matched to its task — exactly what the assessment scenarios typically require.", bloom: "create" },
  ],

  // ─── Topic 6: Sorting, Filtering, and Linking Sheets ───
  6: [
    { q: "Which statement correctly describes filtering vs sorting?", opts: ["They are the same operation", "Sorting reorders rows based on a column's values (all rows still visible). Filtering hides rows that don't match a condition (hidden rows still exist in the file)", "Sorting deletes rows; filtering keeps them", "Filtering reorders rows; sorting hides them"], ans: 1, why: "Sort = reorder, no data lost. Filter = hide, no data lost. Both are non-destructive; mixing them up is the most common student confusion.", bloom: "remember" },
    { q: "Why is filtering considered \"non-destructive\" even when rows disappear from view?", opts: ["Because Excel secretly deletes them and restores on save", "Hidden rows still exist in the file — the filter only changes the view. Clearing the filter brings every row back. Save-and-reopen also preserves the data", "Because you can undo once", "Because filtering doesn't actually work"], ans: 1, why: "Understanding filtering means separating view from data. The rows are still present, just hidden. This is why sharing a filtered file can confuse recipients who don't notice the filter is active.", bloom: "understand" },
    { q: "You want to see only students in Section A who scored above 80. Which sequence of operations?", opts: ["Delete every other row manually", "Apply a filter to the Section column (= \"A\"), then add a filter to the Score column (>80). The filtered view shows only matching rows", "Sort by section alphabetically and read top 10", "Copy all rows to a new sheet"], ans: 1, why: "Applying filter knowledge: multiple filters combine with AND logic. Two filters narrow the view to rows matching both conditions. Sorting wouldn't hide non-A or ≤80 rows.", bloom: "apply" },
    { q: "Sheet \"Summary\" needs to pull the grand total from cell B100 of sheet \"Sales Q1\" (with a space in the name). What's the correct reference?", opts: ["=Sales Q1!B100", "='Sales Q1'!B100", "=Sales_Q1!B100", "=\"Sales Q1\".B100"], ans: 1, why: "Applying cross-sheet reference syntax: when the sheet name has spaces or special characters, wrap the name in single quotes, then `!`, then the cell. Without quotes, Excel parses the space as end-of-name and errors.", bloom: "apply" },
    { q: "A team shares a spreadsheet where sorting is done in place (no copy). A colleague opens it, sorts by a different column, and saves. Analyse the consequence for anyone who had been working with the original order.", opts: ["No consequence — sorts undo themselves", "The saved file's row order is now different. Anyone relying on the \"original\" order (say, row 5 was \"my assigned task\") will find their reference has moved. In-place sorts on shared files are a hidden source of confusion", "Only the colleague's copy changes", "Sorting is impossible on shared files"], ans: 1, why: "Analysing sort in shared contexts means seeing that saved state is shared state. Row positions shouldn't carry meaning in a shared sheet precisely because anyone can resort. Meaning should live in the data (IDs, names), not the row position.", bloom: "analyze" },
    { q: "A manager debates whether to replace cross-sheet linking with copy-paste values (\"just paste the numbers — simpler\"). Evaluate.", opts: ["Copy-paste is always better", "Linking maintains a single source of truth: update the source, every dependent sheet updates. Copy-paste creates drift — the moment the source changes, pasted copies become stale. Copy-paste is acceptable only for snapshots/archives", "Linking is always better regardless of scenario", "They're equivalent"], ans: 1, why: "Evaluating means naming the trade-off (live reference vs frozen snapshot). Linking wins for live data; copy-paste wins for archives. The \"simpler\" argument sounds good but costs correctness.", bloom: "evaluate" },
    { q: "Design a structure for a monthly sales dashboard that pulls from 12 monthly sheets. How do you link them so that adding a new month is painless?", opts: ["Retype the numbers on the dashboard each month", "Dashboard sheet with formulas like `='Jan'!B100`, `='Feb'!B100`, etc. For scalability, use a lookup column (month name) and INDIRECT: `=INDIRECT(\"'\"&A2&\"'!B100\")`. Adding a new month only means adding its name to the lookup, not editing the dashboard", "One giant copy-paste every month", "Only link to one sheet, ignore the rest"], ans: 1, why: "Designing a scalable dashboard means building the link once so future additions are cheap. INDIRECT + a lookup column is the pattern; direct references are fine for a fixed set but require editing the dashboard for each addition.", bloom: "create" },
  ],

  // ─── Topic 7: PowerPoint — Slides, Themes, Layouts ───
  7: [
    { q: "Which layout is specifically designed for the opening slide of a presentation?", opts: ["Two Content", "Title Slide", "Section Header", "Blank"], ans: 1, why: "Title Slide = large centred title and subtitle, meant to be slide 1. Section Header is for dividers mid-deck. Two Content / Blank serve different purposes.", bloom: "remember" },
    { q: "Why does applying a Theme change every slide at once, rather than just the current one?", opts: ["A bug in PowerPoint", "A Theme defines the deck's global design (colour palette, fonts, backgrounds). Making it per-slide would defeat the goal of visual consistency and multiply maintenance work", "Themes only work on one slide at a time", "Themes are random"], ans: 1, why: "Understanding Themes means seeing them as a global style contract. Consistency across slides is the entire point; per-slide themes would be Slide Master light, which is what Slide Master already is.", bloom: "understand" },
    { q: "You want to compare \"Manual Process\" and \"Automated Process\" side by side with bullet points under each. Which layout fits?", opts: ["Title Slide", "Two Content", "Section Header", "Blank"], ans: 1, why: "Applying layout fit: Two Content gives a title on top and two equal content panes — exactly the structure for side-by-side comparison.", bloom: "apply" },
    { q: "A presenter pastes 400 words of raw lecture notes onto a single slide. Apply the 6×6 rule: what's the problem and what's the fix?", opts: ["No problem — more content = more information", "The 6×6 rule (max 6 bullets, ~6 words each) exists because the audience reads faster than you speak, stops listening, and walks away with nothing. Fix: strip to 3–6 short bullets and put detail into speaker notes", "The slide is too small", "Just make the font smaller"], ans: 1, why: "Applying the rule means diagnosing the pattern (too much text → reading competes with listening → retention collapses) and offering the right fix (move detail to speaker notes).", bloom: "apply" },
    { q: "A student prepares for a 5-minute class presentation with 30 slides. Analyse whether this slide count is appropriate.", opts: ["Perfect — more slides mean more content", "Too many. Rule of thumb: ~1–2 minutes per slide for real engagement. 30 slides in 5 minutes = 10 seconds each, which is too fast for audience comprehension. Target 3–5 content slides + title/closing", "Too few — should be 60", "Slide count is irrelevant"], ans: 1, why: "Analysing time budget: content × duration must balance. 10 seconds per slide is TikTok speed, not a lecture. Good analysis produces a realistic count range.", bloom: "analyze" },
    { q: "A designer proposes using 5 different fonts across a 20-slide deck \"for visual interest.\" Evaluate.", opts: ["Great — variety is good", "Flawed. Typographic consistency (2 fonts: one for headings, one for body) signals polish and lets content be the star. 5 fonts make the deck look amateur and distract from the message. Visual interest should come from images, charts, whitespace — not font chaos", "Great — 10 fonts would be even better", "Doesn't matter in PowerPoint"], ans: 1, why: "Evaluating design choices means naming the principle (consistency → credibility) and showing 5 fonts violates it. The right counterproposal (1-2 fonts + imagery for interest) is the standard.", bloom: "evaluate" },
    { q: "Design the slide outline for a 10-minute board presentation on quarterly sales results. How many slides, in what order, with what layouts?", opts: ["30 slides of raw data", "~6 slides: (1) Title Slide, (2) Section Header \"Highlights\", (3) Title+Content with top 3 metrics as a chart, (4) Title+Content with trend analysis, (5) Two Content comparing best and worst performer, (6) Title+Content with recommendations + Q&A. 10 min / 6 slides ≈ 1.5 min each", "1 slide with all numbers", "50 slides of appendix"], ans: 1, why: "Designing a presentation outline means producing a slide count, an order, and a layout per slide that fits the time budget AND serves the audience. A designed answer should name each slide's purpose.", bloom: "create" },
  ],

  // ─── Topic 8: Slide Master, Transitions, Animations, Shortcuts ───
  8: [
    { q: "What does the Slide Master control?", opts: ["Only the first slide", "The default fonts, colours, backgrounds, and placeholders for ALL slides based on it — edit once, apply everywhere", "The slide show timings only", "The speaker notes font only"], ans: 1, why: "Slide Master is the top-level template. Layout Masters beneath it inherit its styling. Change a logo on the master once, every slide updates.", bloom: "remember" },
    { q: "Why is the distinction between Transition and Animation important, and how do they differ?", opts: ["They're the same thing with different names", "Transitions play between slides (fade, wipe from slide A to slide B). Animations play within a slide (a bullet flying in, an image bouncing). Mixing them up wastes time configuring the wrong effect in the wrong tab", "Transitions only work on the first slide", "Animations only work on text"], ans: 1, why: "Understanding the difference means recognising their scopes. Transition = between. Animation = within. Two different tabs, two different purposes.", bloom: "understand" },
    { q: "You want to start a slideshow from slide 1 for an audience. Which shortcut?", opts: ["F5", "Shift+F5", "Ctrl+F5", "Alt+F5"], ans: 0, why: "Applying the shortcut: F5 = start from the beginning. Shift+F5 = start from current slide (useful when rehearsing a single section).", bloom: "apply" },
    { q: "You need to add your company's logo to every slide of a 50-slide deck. What's the efficient way?", opts: ["Paste it onto each slide individually", "Open Slide Master (View → Slide Master), paste the logo onto the top Master slide once — every slide inherits it. Closing Slide Master view applies across all existing slides", "Email the logo to yourself", "Logos can't go on all slides"], ans: 1, why: "Applying Slide Master knowledge: the whole point is \"edit once, apply everywhere.\" Pasting 50 times is the wrong instinct when the tool exists exactly to avoid that.", bloom: "apply" },
    { q: "A presenter reports that their deck looks fine on their laptop but fonts change when opened on the conference-room PC. Analyse the cause.", opts: ["The conference PC is broken", "The deck uses fonts installed only on the presenter's laptop. When PowerPoint opens the file elsewhere and the font is missing, it substitutes a default. Fix: embed fonts (File → Options → Save → Embed fonts) or stick to common fonts (Calibri, Arial, Times New Roman)", "PowerPoint randomises fonts", "The deck is corrupted"], ans: 1, why: "Analysing font portability means identifying where fonts live (on the OS, not in the file by default) and how to prevent substitution. It's one of the top reasons \"it worked on my machine\" fails in presentations.", bloom: "analyze" },
    { q: "A designer adds 8 different animations on every slide \"to keep the audience engaged.\" Evaluate.", opts: ["Great — busy is engaging", "Counterproductive. Excessive animation distracts from content, wastes time as the audience waits for text to finish flying in, and looks amateur. Animations should be purposeful (build a chart step by step, reveal a surprise) — not decorative", "Great — more is always better", "Doesn't matter"], ans: 1, why: "Evaluating animation usage means naming the difference between PURPOSEFUL (builds understanding) and DECORATIVE (wastes attention). The strong answer calls out the audience-cost of pointless motion.", bloom: "evaluate" },
    { q: "Design the Slide Master setup for a consulting firm's standard template. What elements go on the master vs on individual slides?", opts: ["Everything on individual slides", "Master: company logo (top-right), client name placeholder (top-left), page number (bottom-right), brand colour palette, standard fonts (heading + body), confidentiality footer. Individual slides: actual content per slide. Result: consistent branding without per-slide maintenance", "No master — freestyle every slide", "Put the whole presentation on the master"], ans: 1, why: "Designing a Slide Master means deciding what's global (brand elements) vs per-slide (content). A firm template should standardise branding while leaving content free. Listing both sets shows real understanding.", bloom: "create" },
  ],

  // ─── Topic 9: Choosing the Right Tool (Word vs Excel vs PowerPoint) ───
  9: [
    { q: "What's the \"golden rule\" for picking Word, Excel, or PowerPoint?", opts: ["Always use Word", "Word for documents (writing/reading), Excel for calculations and tabular data, PowerPoint for presentations (visual delivery to an audience)", "Always use Excel", "The tools are interchangeable"], ans: 1, why: "Tool fit follows purpose: writing → Word, numbers → Excel, presenting → PowerPoint. The easiest mistake is forcing one job onto the wrong tool.", bloom: "remember" },
    { q: "Why do people still reach for Word for a resume instead of a visual tool like Canva or PowerPoint?", opts: ["Word is cheaper", "Resumes are documents read by humans AND screened by Applicant Tracking Systems (ATS). Word's text-first format is parseable by ATS; heavily visual tools often produce PDFs with text-as-images that ATS can't read", "PowerPoint has no text feature", "Canva doesn't exist"], ans: 1, why: "Understanding tool fit means considering the downstream reader (ATS) in addition to the human reader. That constraint makes Word (or plain-text exports) safer than design-forward tools for resumes going through recruiters.", bloom: "understand" },
    { q: "HR needs to track attendance for 300 employees with sort/filter/formula capabilities. Which tool?", opts: ["Word with a table", "Excel — large tabular data with calculations, sorting, and filtering is exactly what spreadsheets are for", "PowerPoint with text boxes", "Notepad"], ans: 1, why: "Applying the golden rule: tabular + calculations = Excel. Word tables can't sort, filter, or compute at scale. PowerPoint is the wrong universe entirely.", bloom: "apply" },
    { q: "A startup founder has to (a) write an investor pitch document, (b) build a 3-year revenue forecast, (c) present to the investors live. Map each task to a tool.", opts: ["All three in PowerPoint", "(a) Word — written document. (b) Excel — numeric forecasts with formulas. (c) PowerPoint — live audience. Then embed/link the Excel charts into both the Word doc and the PowerPoint deck", "All three in Excel", "All three in Word"], ans: 1, why: "Applying tool fit per task. A bonus insight: Office apps integrate, so the Excel forecast can live in both the Word doc and the PowerPoint deck as linked objects — single source of truth across tools.", bloom: "apply" },
    { q: "A company uses Excel for writing all internal memos (\"one cell per paragraph\"). Analyse the downstream problems.", opts: ["No problems — Excel can hold text", "Multiple problems: (1) no formatting continuity (spell-check per cell, no paragraph styles), (2) printing is ugly (row heights don't match prose), (3) no navigation or TOC, (4) collaboration comments are awkward. Memos are prose — they belong in a document tool, not a grid", "Excel is always best for text", "It will make files smaller"], ans: 1, why: "Analysing the misuse means listing the specific capabilities lost (formatting, printing, navigation, collaboration). Tools have specialities; forcing prose into a grid forfeits all the reasons we write prose in the first place.", bloom: "analyze" },
    { q: "A university argues: \"Let's standardise on Google Docs for ALL output — documents, spreadsheets, presentations — since it has all three.\" Evaluate.", opts: ["Brilliant — consistency wins", "Partly correct. Docs-Sheets-Slides does cover the three tool categories and supports collaboration well. But Sheets is weaker than Excel for large/complex spreadsheets, and Slides has fewer animation/transition options than PowerPoint. For most students and small teams, the trade-off is fine; for power users it's constraining", "Terrible — Google has no office tools", "Doesn't matter"], ans: 1, why: "Evaluating the proposal means giving a nuanced answer: the Google suite covers the three categories (so the golden rule still applies) but loses power-user features in Sheets and Slides. A blanket yes or no misses the trade-off.", bloom: "evaluate" },
    { q: "Design a document-production workflow for a consulting team that writes 50-page reports with many charts and presents summarised findings to clients. Name which tool does what and how they connect.", opts: ["Everything in Word", "Excel: source of all data and charts (single source of truth). Word: the 50-page report, with Excel charts embedded as linked objects. PowerPoint: the client presentation, also embedding the same Excel charts. Updating Excel once updates both. Each tool does its core job; integration keeps data consistent", "Everything in Excel", "No workflow — random tool choice"], ans: 1, why: "Designing a multi-tool workflow means naming each tool's role AND how they stay in sync. Linked Excel charts in Word and PowerPoint is the classic consulting pattern precisely because it avoids drift between the report and the deck.", bloom: "create" },
  ],
};
