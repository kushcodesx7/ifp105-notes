// Module 2 — Office Tools (Word, Excel, PowerPoint)
// Bloom's-aligned MCQs. Each topic follows the progression:
// Q1 Remember · Q2 Understand · Q3 Apply · Q4 Apply · Q5 Analyze · Q6 Evaluate · Q7 Create
// All distractors are Grade A: every option is plausible.

import type { Question } from "./module1-mcq";

export const mcqData: Record<number, Question[]> = {
  // ─── Topic 1: Editing vs Word Processing ───
  1: [
    { q: "Which statement most precisely distinguishes a text editor from a word processor?", opts: ["They are synonyms for the same software category","A text editor handles plain characters; a word processor adds formatting and layout","A word processor saves smaller files than a text editor","Only text editors can open .docx files"], ans: 1, why: "A word processor is editing PLUS formatting, layout, images, tables, and styling — a superset, not a separate tool. Editing (type, delete, copy) is the common ground. Word processing is editing plus everything else.", bloom: "understand" },
    { q: "Why do programmers often prefer a plain text editor like Notepad or VS Code over MS Word for writing code?", opts: ["Word inserts invisible formatting that silently breaks code","Word is too slow","Programmers don't like icons","Code files are too big for Word"], ans: 0, why: "Code must be exact plain text — smart quotes, auto-capitalised letters, and embedded styles that word processors insert will break compilers. A plain text editor guarantees byte-for-byte what you typed. Word's \"features\" (auto-correct, smart quotes) are bugs for code.", bloom: "understand" },
    { q: "Your professor accepts only `.docx` submissions with a header, page numbers, and a table of contents. Applying what you know, which tool fits?", opts: ["A word processor like MS Word or Google Docs","Notepad — it's the simplest option","Either — they produce the same file","Any, but save as .txt"], ans: 0, why: "Headers, pagination, and TOC are word-processing features that plain editors don't support. Applying what you know means matching submission requirements to tool capability. MS Word, Google Docs, or LibreOffice Writer all qualify.", bloom: "apply" },
    { q: "You receive a 2000-word document as a `.txt` file. The content is fine, but it looks \"flat.\" What's happening?", opts: ["The file is corrupted","`.txt` stores plain characters only, so formatting is stripped","Your computer is slow","Only .docx files can hold text"], ans: 1, why: "`.txt` = characters only, no styling metadata. Any bold/italic/colour/heading applied by the original author is lost on save because the format has no way to represent formatting. The content survives; the presentation doesn't.", bloom: "apply" },
    { q: "A student submits a 10-page essay written in Notepad and saved as .txt. Their professor says the formatting is \"unreadable.\" Analyse what the student likely failed to understand about the tool.", opts: ["Notepad has a secret formatting mode that needs to be switched on","The student confused editing with word processing","Universities reject .txt files because the format is too old","Notepad files can't hold more than a few pages of text"], ans: 1, why: "The failure mode is conceptual, not technical. Notepad can hold the words but can't produce headings, consistent fonts, spacing, or pagination the professor expected. The student used a tool sized for characters when the task required presentation — that's the editing vs word-processing distinction in practice.", bloom: "analyze" },
    { q: "A company standardises on Google Docs for everything and bans plain text editors entirely. Evaluate this policy.", opts: ["Great — Docs does everything","Great — consistency is all that matters","Flawed for roles that handle code, config files, CSVs, or logs","Flawed only because Docs needs internet"], ans: 2, why: "Docs can auto-format and corrupt plain-text content (code, CSVs, config, logs). The policy serves writers but creates friction for engineers, finance, and ops. Evaluating a policy means testing it against actual roles — a blanket ban ignores that some jobs need exactness, not presentation.", bloom: "evaluate" },
    { q: "Design a file-format policy for a university department that handles: research papers, course notes, code samples, and meeting minutes. Which format(s) would you require?", opts: ["Papers .docx/.pdf, notes .docx/Markdown, code .txt, minutes .docx/Markdown","Everything as .docx","Everything as .txt","Everything as PDF only"], ans: 0, why: "Each format is chosen for fidelity to its content: papers need rich formatting (.docx/.pdf), course notes work in .docx or Markdown, code must be plain text (never .docx), minutes work in either. Designing a policy means mapping content type to format capability. A one-size answer fails; a per-content answer shows you understand the editing vs word-processing distinction.", bloom: "create" },
  ],

  // ─── Topic 2: Word Formatting (Ribbon, shortcuts, alignment) ───
  2: [
    { q: "What is the Ribbon in Microsoft Word?", opts: ["The document outline at the left","The bottom status bar showing word count","The tabbed strip at the top grouping related tools","The scroll bar along the right edge"], ans: 2, why: "The Ribbon is Word's primary tool container — tabs across the top (Home, Insert, Layout, etc.), each with groups of related buttons. The other answers describe different UI elements.", bloom: "remember" },
    { q: "Why does Word (and most apps) require you to SELECT text before applying bold, italic, or a different font?", opts: ["Word is old and requires extra steps","Selection makes Word faster","Selection is optional — formatting works without it","A command needs a target to act on"], ans: 3, why: "Without a selection, Word has no target; with a selection, the command knows exactly what range to modify. It's an unambiguous contract between user and program. \"Select → command\" is a general UI pattern, not a Word quirk — any command that changes content has to know WHAT content it's changing.", bloom: "understand" },
    { q: "A heading should stretch evenly across the full line width, with both left and right edges perfectly aligned. Which alignment and shortcut?", opts: ["Centre (Ctrl+E)","Right (Ctrl+R)","Justify (Ctrl+J)","Left (Ctrl+L)"], ans: 2, why: "Justify stretches word spacing so both edges align. Centre keeps the line centred but doesn't stretch. Left/right align only one edge.", bloom: "apply" },
    { q: "You've formatted a paragraph exactly how you want it (font, colour, spacing, size) and need to apply the same styling to six other paragraphs fast. What's the most efficient approach?", opts: ["Retype every paragraph with the same settings","Use Format Painter to copy formatting to the targets","Copy and paste each paragraph then edit content back","Change each paragraph manually one at a time"], ans: 1, why: "Format Painter is literally designed to duplicate formatting across selections — click on the formatted paragraph, then click/drag across the targets. It copies formatting without changing content — faster and more consistent than any manual approach.", bloom: "apply" },
    { q: "A document uses manual bold+size+font combinations on every heading instead of the built-in Heading 1/Heading 2 styles. Analyse the downstream consequences.", opts: ["No consequences — it looks the same","Inconsistent styling, no auto TOC, no navigation, no global restyle","The document will get corrupted","Word will refuse to save the file"], ans: 1, why: "Styles aren't just formatting — they're metadata. Abandoning them forfeits TOC generation, navigation pane, global restyling, and outline view. Restyling the whole document means editing every heading individually. Analysing means tracing the ripple effects.", bloom: "analyze" },
    { q: "A team debates whether to ban inline formatting in favour of Styles-only. Evaluate.", opts: ["Styles-only suits long structured docs; inline is fine for short notes","Inline formatting is always fine","Ban styles — inline is simpler","Both are identical in practice"], ans: 0, why: "Styles pay off in proportion to document length and structure — they enable TOC, outline navigation, and global restyle. A one-page note doesn't need them; a 50-page report does. A total ban may be too strict for casual notes, but recommending Styles for structured work is correct. A nuanced policy beats a blanket rule.", bloom: "evaluate" },
    { q: "Design a style system for a 100-page university thesis. Name the styles you would define and explain why each exists.", opts: ["Only one style for everything","One style per page","Title, H1, H2, H3, Body, Caption, Block Quote, Code","Bold and italic, nothing else"], ans: 2, why: "Each style encodes a structural role: Title, Heading 1 (chapters), Heading 2 (sections), Heading 3 (subsections), Body, Caption (figures/tables), Block Quote, Code. The document becomes globally restyleable and the TOC auto-generates. Naming matches Word's built-in hierarchy so it \"just works.\" A flat \"one style\" approach loses all structural benefits.", bloom: "create" },
  ],

  // ─── Topic 3: Images and Tables in Word ───
  3: [
    { q: "Which Ribbon tab hosts the tools for inserting images, tables, shapes, and charts?", opts: ["Home","Layout","Insert","View"], ans: 2, why: "The Insert tab is the designated home for adding anything that isn't plain typed text. Home is formatting; Layout is page setup; View is display preferences.", bloom: "remember" },
    { q: "Why should you drag a CORNER handle (not a side handle) when resizing an image?", opts: ["Corner handles preserve the aspect ratio","Corner handles are more reliable","Side handles don't work in modern Word","There is no difference"], ans: 0, why: "Corner = proportional resize (width-to-height ratio preserved); side = stretch one dimension, distorting the image. The visible difference shows up immediately on photos — stretched people get visibly squashed.", bloom: "understand" },
    { q: "You insert a company logo at the top of a page and want the paragraph text to flow around it on the right. Which text wrapping setting?", opts: ["Square","In Line with Text","Behind Text","In Front of Text"], ans: 0, why: "Square wraps text around the image's bounding box. \"In Line\" treats the image as a big character (breaks flow). \"Behind/In Front\" overlaps, which is usually wrong for a logo + text layout.", bloom: "apply" },
    { q: "You need a schedule table: 7 columns (days) and 10 rows (hour slots), with header highlighting. What's the most reliable way to build it?", opts: ["Insert → Table → Insert Table (7×10), then shade the header row","Draw each line with the pen tool","Use spaces and the Enter key to align columns","Export from Excel only"], ans: 0, why: "The Insert Table dialog lets you specify exact dimensions (grid mode caps at 10×8). Styling the header via Table Design is the standard path.", bloom: "apply" },
    { q: "A student uses a table with invisible borders to lay out their whole page (navigation row, content row, footer row). Analyse why this is considered bad practice today.", opts: ["It isn't — it works fine","It will crash Word","Tables only work in HTML, not Word","Tables are for tabular data; using them for layout breaks semantics"], ans: 3, why: "Tables-for-layout confuses screen readers (\"row 1 contains 3 cells of unrelated content\"), breaks copy-paste ordering, and fights modern responsive design. The visual looks fine; the semantics are wrong. It's a historical web hack that also hurts Word document accessibility — the file opens, but assistive tech misreads it.", bloom: "analyze" },
    { q: "A designer argues that every image in a report should use \"In Line with Text\" wrapping because it's \"simplest and least likely to break.\" Evaluate.", opts: ["Always correct — In Line is safest","Doesn't matter which you pick","Always wrong — never use In Line","Partly correct — fine for small inline icons, wrong for large figures"], ans: 3, why: "In Line IS the most predictable because the image behaves like a character — safe for small inline icons. For large figures you want text to flow around, Square/Tight is better. \"Simplest\" is often right, but \"always\" is too strong — it's a rule of thumb, not absolute.", bloom: "evaluate" },
    { q: "Design the table structure for a 3-page course syllabus that lists week number, topic, readings, and assignments. How do you structure the table, and how do you ensure it prints well across pages?", opts: ["4 columns with a styled header row and \"Repeat Header Rows\" enabled","One giant cell with all the data","A table per row","Paragraphs with spaces"], ans: 0, why: "Columns: Week, Topic, Readings, Assignments. Enable \"Repeat Header Rows\" in Table Properties so the header re-prints on each page; use consistent cell padding. Designing a data table means naming the columns, styling the header, and solving the print-flow problem. A simple-looking answer that ignores page breaks isn't a complete design.", bloom: "create" },
  ],

  // ─── Topic 4: Excel — Cells, Rows, Columns, Sheets ───
  4: [
    { q: "What does \"cell C5\" refer to in Excel?", opts: ["The 5th cell of column C","The cell at column C, row 5","Row C, column 5","The 5th sheet named C"], ans: 1, why: "Excel addresses are [column letter][row number]. C = third column. 5 = fifth row. Always column first, then row.", bloom: "remember" },
    { q: "Why does Excel organise data into separate Worksheets within a single Workbook, instead of just one giant sheet?", opts: ["It's a limitation of older Excel versions","Separation: each topic gets its own sheet in one file","Worksheets can't actually hold much data","Workbooks are a legacy feature"], ans: 1, why: "Different topics (sales, inventory, forecasts) get their own sheet with their own structure, while sharing one file. Linking formulas can pull data across sheets when needed. It's a file-and-tabs pattern: one file for organisation, many sheets for logical separation.", bloom: "understand" },
    { q: "You need to sum cells from B2 through B30. Which range notation is correct?", opts: ["=SUM(B2:B30)","=SUM(B2, B30)","=SUM(B2-B30)","=SUM(B2+B30)"], ans: 0, why: "The colon creates a range (all cells from B2 to B30 inclusive). Comma = list of individual cells. Dash/plus = arithmetic, not range.", bloom: "apply" },
    { q: "You entered \"45\" into A1 but it left-aligns and won't sum. What's happening and how do you fix it?", opts: ["The cell is formatted as Text; reformat to Number","Excel is broken","45 is too small a number","The keyboard is in the wrong language"], ans: 0, why: "Left-alignment + refusing to sum = classic \"looks like a number but is text.\" \"45\" is treated as characters, not a number. Fix: select the cell → Format Cells → Number, or retype the number in a fresh General-formatted cell. Using =VALUE() also works.", bloom: "apply" },
    { q: "A workbook has three sheets: Sales, Expenses, Summary. The Summary sheet pulls totals from the other two using `=Sales!B10` and `=Expenses!B10`. Someone renames \"Sales\" to \"Q1 Sales.\" Analyse what happens.", opts: ["Nothing — Excel figures it out","All formulas break forever","It depends on how the rename was done and if the name has spaces","The file gets corrupted"], ans: 2, why: "Excel automatically updates references IF the rename is done via the tab itself. If the sheet was renamed externally or the file is opened in an older app, the references break and show `#REF!`. Spaces in names also require single quotes: `='Q1 Sales'!B10`. Real knowledge of the mechanism, not just syntax.", bloom: "analyze" },
    { q: "A teacher puts 300 students in a single worksheet with 50 columns of data per student. Evaluate whether this is a good structure.", opts: ["Perfect — all data in one place","Should be on 300 separate sheets","Terrible — Excel can't hold 300 rows","Rows are fine; 50 columns is the real concern"], ans: 3, why: "Excel handles millions of rows — row count isn't the issue. Column sprawl is: if columns represent different categories (grades, attendance, demographics), splitting into multiple sheets or using a proper database is easier to maintain. Evaluate per-column purpose, not just row count.", bloom: "evaluate" },
    { q: "Design the workbook structure for a small business tracking orders, customers, and products. How many sheets, and how do they relate?", opts: ["Everything on one sheet","Three sheets — Customers, Products, Orders — linked by ID","One sheet per order","No structure — random"], ans: 1, why: "Customers (ID, name, email), Products (ID, name, price), Orders (order ID, customer ID, product ID, qty, date). Orders references the other two by ID. This mirrors a relational database and keeps each sheet focused. A single-sheet blob duplicates customer/product data on every order row; a per-order sheet is unusable. The three-sheet structure is the industry pattern.", bloom: "create" },
  ],

  // ─── Topic 5: Excel Formulas (SUM, AVERAGE, IF, MIN, MAX, COUNT) ───
  5: [
    { q: "Every Excel formula must start with which character, and why?", opts: ["A number, so Excel treats it as data","An equals sign (=) so Excel evaluates it","A quotation mark, to group it","A hashtag, to categorise it"], ans: 1, why: "= tells Excel that what follows is a formula to evaluate, not text to store. Without it, `A1+B1` displays as literal text.", bloom: "understand" },
    { q: "Why does `=AVERAGE(A1:A10)` give a different result from `=SUM(A1:A10)/10` if some cells in A1:A10 are empty?", opts: ["They are always identical","AVERAGE divides by count of numeric cells; /10 divides by 10","SUM ignores empty cells; AVERAGE doesn't","Only AVERAGE works with ranges"], ans: 1, why: "If 3 of the 10 cells are empty, AVERAGE divides by 7 and /10 divides by 10 — two different answers. AVERAGE = sum / count-of-numbers; the /10 hardcodes a denominator that may not match reality. For data with blanks, AVERAGE is correct; /10 is a bug.", bloom: "understand" },
    { q: "A teacher has scores in B2:B31 and needs to count how many are 40 or above (pass). Which formula is correct?", opts: ["=COUNT(B2:B31)","=IF(B2:B31>=40, 1, 0)","=SUMIF(B2:B31, \">=40\")","=COUNTIF(B2:B31, \">=40\")"], ans: 3, why: "COUNT counts all numeric cells. SUMIF adds values meeting a condition (gives sum, not count). COUNTIF counts cells meeting a condition — exactly what's asked.", bloom: "apply" },
    { q: "Cell D1 = `=IF(A1>100, \"Gold\", IF(A1>50, \"Silver\", \"Bronze\"))`. A1 contains 75. What does D1 show, and why?", opts: ["Gold, because 75 is a large number","Bronze — nothing matched","Silver — the inner IF matches 75>50","#ERROR — nested IFs aren't allowed"], ans: 2, why: "The first IF checks 75>100 (false), falls to the inner IF which checks 75>50 (true), returns \"Silver\". In nested IFs, conditions are checked in order, and the first true one wins. Real knowledge means tracing logic step by step, not eyeballing.", bloom: "apply" },
    { q: "A payroll sheet uses `=B2*C2` for every row, copied down 500 rows. Someone adds a new \"Bonus\" column between B and C, shifting C to D. Analyse what happens to the formulas.", opts: ["Excel updates references to `=B2*D2` automatically","Nothing — formulas break","All formulas point to the same cell","Excel refuses to insert the column"], ans: 0, why: "Excel tracks cell identity through column insertions, so formulas become `=B2*D2`. But any NEW C column content is ignored — the formulas still multiply the original columns, not the new structure. This is correct behaviour (Excel preserves reference semantics, not positions), but easy to misread as a bug by people who think of formulas as position-based.", bloom: "analyze" },
    { q: "A team uses `=IF(score>=60, \"Pass\", \"Fail\")` everywhere. A manager proposes switching to `=IFS(score>=85, \"A\", score>=70, \"B\", score>=60, \"C\", TRUE, \"F\")`. Evaluate.", opts: ["The simple IF is always better","They're identical — pick any","Always use the longest formula","IFS suits multi-branch logic; IF is fine for two outcomes"], ans: 3, why: "IFS is cleaner than deeply nested IFs, easier to read, and scales to 5+ conditions. For just two outcomes (Pass/Fail), IF is fine — it depends on the decision shape. Binary → IF. Multi-branch → IFS (or nested IF, if forced). Match the tool to the decision shape.", bloom: "evaluate" },
    { q: "Design a set of formulas for a grade sheet with 30 students' scores in B2:B31. You need: average, highest, lowest, number of passes (≥60), percentage passing, and a letter-grade column.", opts: ["Use AVERAGE, MAX, MIN, COUNTIF, and IFS — one per task","Just use SUM for everything","Write the answers by hand","Use =TOTAL for everything"], ans: 0, why: "Average: =AVERAGE(B2:B31). Highest: =MAX(B2:B31). Lowest: =MIN(B2:B31). Passes: =COUNTIF(B2:B31,\">=60\"). % passing: =COUNTIF(B2:B31,\">=60\")/COUNT(B2:B31). Letter grade (in C2, dragged): =IFS(B2>=85,\"A\",B2>=70,\"B\",B2>=60,\"C\",TRUE,\"F\"). Five distinct functions, each matched to its task.", bloom: "create" },
  ],

  // ─── Topic 6: Sorting, Filtering, and Linking Sheets ───
  6: [
    { q: "Which statement correctly describes filtering vs sorting?", opts: ["They are the same operation","Sorting deletes rows; filtering keeps them","Sorting reorders rows; filtering hides them","Filtering reorders rows; sorting hides them"], ans: 2, why: "Sort = reorder, no data lost (all rows still visible). Filter = hide rows not matching a condition, no data lost (hidden rows still exist). Both are non-destructive; mixing them up is the most common student confusion.", bloom: "remember" },
    { q: "Why is filtering considered \"non-destructive\" even when rows disappear from view?", opts: ["Because Excel secretly deletes them and restores on save","Because you can undo once","Hidden rows still exist — only the view changes","Because filtering doesn't actually work"], ans: 2, why: "The filter only changes the view. Clearing the filter brings every row back. Save-and-reopen also preserves the data. Filtering separates view from data. This is why sharing a filtered file can confuse recipients who don't notice the filter is active.", bloom: "understand" },
    { q: "You want to see only students in Section A who scored above 80. Which sequence of operations?", opts: ["Delete every other row manually","Sort by section alphabetically and read top 10","Apply filters on Section (=\"A\") and Score (>80)","Copy all rows to a new sheet"], ans: 2, why: "Multiple filters combine with AND logic. Two filters narrow the view to rows matching both conditions. Sorting wouldn't hide non-A or ≤80 rows.", bloom: "apply" },
    { q: "Sheet \"Summary\" needs to pull the grand total from cell B100 of sheet \"Sales Q1\" (with a space in the name). What's the correct reference?", opts: ["=Sales Q1!B100","='Sales Q1'!B100","=Sales_Q1!B100","=\"Sales Q1\".B100"], ans: 1, why: "When the sheet name has spaces or special characters, wrap the name in single quotes, then `!`, then the cell. Without quotes, Excel parses the space as end-of-name and errors.", bloom: "apply" },
    { q: "A team shares a spreadsheet where sorting is done in place (no copy). A colleague opens it, sorts by a different column, and saves. Analyse the consequence for anyone who had been working with the original order.", opts: ["No consequence — sorts undo themselves","Sorting is impossible on shared files","Only the colleague's copy changes","The saved order changes for everyone; row positions lose meaning"], ans: 3, why: "Anyone relying on the \"original\" order (say, row 5 was \"my assigned task\") will find their reference has moved. In-place sorts on shared files are a hidden source of confusion. Saved state is shared state. Meaning should live in the data (IDs, names), not the row position.", bloom: "analyze" },
    { q: "A manager debates whether to replace cross-sheet linking with copy-paste values (\"just paste the numbers — simpler\"). Evaluate.", opts: ["Copy-paste is always better","Linking keeps one source of truth; copy-paste drifts","Linking is always better regardless of scenario","They're equivalent"], ans: 1, why: "Linking: update the source, every dependent sheet updates. Copy-paste creates drift — the moment the source changes, pasted copies become stale. Copy-paste is acceptable only for snapshots/archives. The \"simpler\" argument sounds good but costs correctness.", bloom: "evaluate" },
    { q: "Design a structure for a monthly sales dashboard that pulls from 12 monthly sheets. How do you link them so that adding a new month is painless?", opts: ["Dashboard with direct links, or INDIRECT for scalability","Retype the numbers on the dashboard each month","One giant copy-paste every month","Only link to one sheet, ignore the rest"], ans: 0, why: "Basic: dashboard sheet with formulas like `='Jan'!B100`, `='Feb'!B100`. For scalability, use a lookup column (month name) and INDIRECT: `=INDIRECT(\"'\"&A2&\"'!B100\")`. Adding a new month only means adding its name to the lookup, not editing the dashboard. Build the link once so future additions are cheap.", bloom: "create" },
  ],

  // ─── Topic 7: PowerPoint — Slides, Themes, Layouts ───
  7: [
    { q: "Which layout is specifically designed for the opening slide of a presentation?", opts: ["Two Content","Blank","Section Header","Title Slide"], ans: 3, why: "Title Slide = large centred title and subtitle, meant to be slide 1. Section Header is for dividers mid-deck. Two Content / Blank serve different purposes.", bloom: "remember" },
    { q: "Why does applying a Theme change every slide at once, rather than just the current one?", opts: ["A Theme defines the deck's global design","A bug in PowerPoint","Themes only work on one slide at a time","Themes are random"], ans: 0, why: "A Theme controls the colour palette, fonts, and backgrounds across all slides. Making it per-slide would defeat the goal of visual consistency and multiply maintenance work. Consistency across slides is the entire point — per-slide themes would be Slide Master light, which is what Slide Master already is.", bloom: "understand" },
    { q: "You want to compare \"Manual Process\" and \"Automated Process\" side by side with bullet points under each. Which layout fits?", opts: ["Two Content","Title Slide","Section Header","Blank"], ans: 0, why: "Two Content gives a title on top and two equal content panes — exactly the structure for side-by-side comparison.", bloom: "apply" },
    { q: "A presenter pastes 400 words of raw lecture notes onto a single slide. Apply the 6×6 rule: what's the problem and what's the fix?", opts: ["No problem — more content = more information","The slide is too small","Too much text — strip to bullets, move detail to speaker notes","Just make the font smaller"], ans: 2, why: "The 6×6 rule caps slides at ~6 bullets of ~6 words each because the audience reads faster than you speak, stops listening, and walks away with nothing. Fix: strip to 3–6 short bullets and put the detail in speaker notes — diagnosis plus the right fix.", bloom: "apply" },
    { q: "A student prepares for a 5-minute class presentation with 30 slides. Analyse whether this slide count is appropriate.", opts: ["Too many — aim for 3–5 content slides plus title/closing","Perfect — more slides mean more content","Too few — should be 60","Slide count is irrelevant"], ans: 0, why: "Rule of thumb: ~1–2 minutes per slide for real engagement. 30 slides in 5 minutes = 10 seconds each — TikTok speed, not a lecture. Content × duration must balance. Good analysis produces a realistic count range.", bloom: "analyze" },
    { q: "A designer proposes using 5 different fonts across a 20-slide deck \"for visual interest.\" Evaluate.", opts: ["Great — variety is good","Flawed — stick to 2 fonts; get interest from images and charts","Great — 10 fonts would be even better","Doesn't matter in PowerPoint"], ans: 1, why: "Typographic consistency (one font for headings, one for body) signals polish and lets content be the star. 5 fonts make the deck look amateur and distract from the message. Visual interest should come from images, charts, and whitespace — not font chaos. Consistency → credibility.", bloom: "evaluate" },
    { q: "Design the slide outline for a 10-minute board presentation on quarterly sales results. How many slides, in what order, with what layouts?", opts: ["30 slides of raw data","1 slide with all numbers","~6 slides pacing roughly 1.5 min each","50 slides of appendix"], ans: 2, why: "Outline: (1) Title Slide, (2) Section Header \"Highlights\", (3) Title+Content with top 3 metrics as a chart, (4) Title+Content with trend analysis, (5) Two Content comparing best and worst performer, (6) Title+Content with recommendations + Q&A. 10 min / 6 slides ≈ 1.5 min each. Name each slide's purpose within the time budget.", bloom: "create" },
  ],

  // ─── Topic 8: Slide Master, Transitions, Animations, Shortcuts ───
  8: [
    { q: "What does the Slide Master control?", opts: ["Default fonts, colours, backgrounds, and placeholders for all slides","Only the first slide","The slide show timings only","The speaker notes font only"], ans: 0, why: "Slide Master is the top-level template — edit once, apply everywhere. Layout Masters beneath it inherit its styling. Change a logo on the master once, every slide updates.", bloom: "remember" },
    { q: "Why is the distinction between Transition and Animation important, and how do they differ?", opts: ["They're the same thing with different names","Transitions play between slides; animations play within a slide","Transitions only work on the first slide","Animations only work on text"], ans: 1, why: "Transition: fade/wipe from slide A to slide B. Animation: a bullet flying in, an image bouncing — effects within a single slide. Mixing them up wastes time configuring the wrong effect in the wrong tab. Two different tabs, two different purposes.", bloom: "understand" },
    { q: "You want to start a slideshow from slide 1 for an audience. Which shortcut?", opts: ["Shift+F5","F5","Ctrl+F5","Alt+F5"], ans: 1, why: "F5 = start from the beginning. Shift+F5 = start from current slide (useful when rehearsing a single section).", bloom: "apply" },
    { q: "You need to add your company's logo to every slide of a 50-slide deck. What's the efficient way?", opts: ["Paste it onto each slide individually","Paste it once onto the Slide Master","Email the logo to yourself","Logos can't go on all slides"], ans: 1, why: "Open Slide Master (View → Slide Master), paste the logo onto the top Master slide — every slide inherits it. Closing Slide Master view applies across all existing slides. The whole point is \"edit once, apply everywhere\" — pasting 50 times is the wrong instinct when the tool exists exactly to avoid that.", bloom: "apply" },
    { q: "A presenter reports that their deck looks fine on their laptop but fonts change when opened on the conference-room PC. Analyse the cause.", opts: ["The conference PC is broken","The deck is corrupted","PowerPoint randomises fonts","Missing fonts on the other PC — embed fonts or use common ones"], ans: 3, why: "Fonts live on the OS, not in the file by default. When PowerPoint opens the file elsewhere and the font is missing, it substitutes a default. Fix: embed fonts (File → Options → Save → Embed fonts) or stick to common fonts (Calibri, Arial, Times New Roman). It's one of the top reasons \"it worked on my machine\" fails in presentations.", bloom: "analyze" },
    { q: "A designer adds 8 different animations on every slide \"to keep the audience engaged.\" Evaluate.", opts: ["Great — busy is engaging","Counterproductive — animations should be purposeful, not decorative","Great — more is always better","Doesn't matter"], ans: 1, why: "Excessive animation distracts from content, wastes time as the audience waits for text to finish flying in, and looks amateur. Animations should BUILD understanding (reveal a chart step by step, surface a surprise) — not decorate. Name the audience-cost of pointless motion.", bloom: "evaluate" },
    { q: "Design the Slide Master setup for a consulting firm's standard template. What elements go on the master vs on individual slides?", opts: ["Everything on individual slides","No master — freestyle every slide","Master holds branding; slides hold content","Put the whole presentation on the master"], ans: 2, why: "Master: company logo (top-right), client name placeholder (top-left), page number (bottom-right), brand colour palette, standard fonts (heading + body), confidentiality footer. Individual slides: actual content per slide. Result: consistent branding without per-slide maintenance. Decide what's global (brand) vs per-slide (content).", bloom: "create" },
  ],

  // ─── Topic 9: Choosing the Right Tool (Word vs Excel vs PowerPoint) ───
  9: [
    { q: "What's the \"golden rule\" for picking Word, Excel, or PowerPoint?", opts: ["Word for documents, Excel for data, PowerPoint for presenting","Always use Word","Always use Excel","The tools are interchangeable"], ans: 0, why: "Word = writing/reading (documents). Excel = calculations and tabular data. PowerPoint = visual delivery to an audience. Tool fit follows purpose; the easiest mistake is forcing one job onto the wrong tool.", bloom: "remember" },
    { q: "Why do people still reach for Word for a resume instead of a visual tool like Canva or PowerPoint?", opts: ["Word is cheaper","Canva doesn't exist","PowerPoint has no text feature","Applicant Tracking Systems can parse Word text but struggle with visual exports"], ans: 3, why: "Resumes are read by humans AND screened by ATS. Word's text-first format is parseable; heavily visual tools often produce PDFs with text-as-images that ATS can't read. Tool fit has to consider the downstream reader (ATS), not just the human.", bloom: "understand" },
    { q: "HR needs to track attendance for 300 employees with sort/filter/formula capabilities. Which tool?", opts: ["Word with a table","PowerPoint with text boxes","Excel","Notepad"], ans: 2, why: "Tabular data + calculations = Excel. Word tables can't sort, filter, or compute at scale. PowerPoint is the wrong universe entirely. Apply the golden rule.", bloom: "apply" },
    { q: "A startup founder has to (a) write an investor pitch document, (b) build a 3-year revenue forecast, (c) present to the investors live. Map each task to a tool.", opts: ["All three in PowerPoint","All three in Word","All three in Excel","Word for (a), Excel for (b), PowerPoint for (c)"], ans: 3, why: "(a) written document → Word. (b) numeric forecasts with formulas → Excel. (c) live audience → PowerPoint. Bonus insight: Office apps integrate, so the Excel forecast can live in both the Word doc and the PowerPoint deck as linked objects — single source of truth across tools.", bloom: "apply" },
    { q: "A company uses Excel for writing all internal memos (\"one cell per paragraph\"). Analyse the downstream problems.", opts: ["No problems — Excel can hold text","It will make files smaller","Excel is always best for text","Broken formatting, ugly printing, no navigation, clumsy collaboration"], ans: 3, why: "Specific losses: (1) no formatting continuity (spell-check per cell, no paragraph styles), (2) printing is ugly (row heights don't match prose), (3) no navigation or TOC, (4) collaboration comments are awkward. Memos are prose — they belong in a document tool, not a grid. Forcing prose into a grid forfeits all the reasons we write prose.", bloom: "analyze" },
    { q: "A university argues: \"Let's standardise on Google Docs for ALL output — documents, spreadsheets, presentations — since it has all three.\" Evaluate.", opts: ["Brilliant — consistency wins","Terrible — Google has no office tools","Partly correct — fine for most, but Sheets and Slides lose power-user features","Doesn't matter"], ans: 2, why: "Docs-Sheets-Slides does cover the three tool categories and supports collaboration well. But Sheets is weaker than Excel for large/complex spreadsheets, and Slides has fewer animation/transition options than PowerPoint. For most students and small teams the trade-off is fine; for power users it's constraining. A blanket yes or no misses the trade-off.", bloom: "evaluate" },
    { q: "Design a document-production workflow for a consulting team that writes 50-page reports with many charts and presents summarised findings to clients. Name which tool does what and how they connect.", opts: ["Excel as data source, linked into Word report and PowerPoint deck","Everything in Word","Everything in Excel","No workflow — random tool choice"], ans: 0, why: "Excel: source of all data and charts (single source of truth). Word: the 50-page report, with Excel charts embedded as linked objects. PowerPoint: the client presentation, also embedding the same Excel charts. Updating Excel once updates both. Each tool does its core job; integration keeps data consistent. The classic consulting pattern — avoids drift between report and deck.", bloom: "create" },
  ],
};
